# web server properties
server.port=8080
server.debug=true
server.static.directories=dist,server/files,.

# database properties
db.uri=mongodb://localhost/fileOrg

# build properties
# concat, minify, uglify...
build.optimize=false