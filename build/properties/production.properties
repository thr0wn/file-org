# web server properties
server.port=80
server.debug=false
server.static.dir=dist

# database properties
db.uri=mongodb://localhost/fileOrg

# build properties
# concat, minify, uglify...
build.optimize=true